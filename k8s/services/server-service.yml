apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: team-disappointment
spec:
  selector:
    app: server
  ports:
    - protocol: TCP
      port: 80
      targetPort: server-api